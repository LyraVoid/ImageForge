/** The marks the Mock Provider writes, so tests and the pipeline can look for them. */
export const MOCK_CMDLINE_MARKER = "imageforge.mock=1";
export const MOCK_BOOTCONFIG_MARKER = "imageforge_mock";
